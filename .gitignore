# OS
Thumbs.db
desktop.ini
.DS_Store
# Python
__pycache__/
*.pyc
# 退避（公開するものではない）
_backup/
_確認用*.html
# ⭐論文の作業ファイル（作り直せるもの）。⚠外から取った書誌の控えだけは git に入れる
_papers/refs一覧.tsv
_papers/論文一覧.json
_フォーカス検査用.html
_撮影用.html

# ⭐⭐⭐ オープンアクセス論文の本文PDF（2026-09-09）
# ⚠⚠この地図は public リポジトリなので、論文本文そのものは絶対に上げない。
#   OAでも再配布の可否はライセンス次第（CC BY / CC BY-NC-ND / 単に無料で読める、が混在する）。
#   ⭐地図からは手元でだけ開ける。公開側では各ノードの DOI から原典へ辿れるようにしてある。
#   ⚠ALGA / LAND は非公開なので PDF を持てる。ここだけ揃えていない。
papers/
# 本文をテキストに起こしたもの（PDFから作り直せる）
_papers/txt/
